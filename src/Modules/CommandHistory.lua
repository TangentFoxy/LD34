--CommandHistory: Another is like a path, for example: SELECT>SET>HEADING>DIRECTION>UP (and it scrolls up away, keeping a few commands of history)
local class = require "lib.middleclass"
local CommandHistory = class("Modules.CommandHistory")
local lg = love.graphics

function CommandHistory:draw(player)
    --
end

return CommandHistory
